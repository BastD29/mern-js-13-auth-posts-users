import {
  actions as authActions,
  // selectors as authSelectors,
  reducer as authReducer,
  // sagaActions as authSagaActions,
} from "./slice2";

export { /* authSagaActions, */ authActions, /* authSelectors, */ authReducer };
